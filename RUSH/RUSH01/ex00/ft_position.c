#include <unistd.h>
#include <stdlib.h>



void	ft_position_up (int x, int y, int h, int l)
{
    while(x < n)
    {
        if(l == 0 && h == 0)
        ft_...
        if(l == 0 && h == x - 1)
        ft...
        if (l == 0 && y == y - 1)
        ft ....
        if (h == 0 && )
    }
}